<li>
  <%= gravatar_for user, size: Settings.gravataruser %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t(".deleted"), user, method: :delete,
      data: { confirm: t(".yousure") } %>
  <% end %>
</li>
